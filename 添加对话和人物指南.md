# 添加对话和人物指南

本指南详细说明如何在《GameDevSolitaire》项目中添加新的对话和新的人物角色。

## 目录
1. [项目结构概览](#项目结构概览)
2. [添加新人物](#添加新人物)
3. [添加新对话](#添加新对话)
4. [高级功能](#高级功能)

---

## 项目结构概览

本项目使用Unity引擎开发，采用基于XNode的对话系统。主要相关文件结构如下：

```
ProjectUnity/Client/
├── Assets/
│   ├── Scripts/
│   │   ├── Dialog/                    # 对话系统核心脚本
│   │   │   ├── DialogGraph.cs         # 对话图表
│   │   │   └── DialogNode.cs          # 对话节点
│   │   ├── Game/Character/             # 角色系统
│   │   │   ├── Character.cs           # 角色类
│   │   │   ├── CharacterCA.cs         # 角色配置数据
│   │   │   └── CharacterFactory.cs    # 角色工厂
│   │   └── Panel/Dialog/
│   │       └── DialogPanel.cs         # 对话面板UI
│   ├── Resources/
│   │   ├── Dialog/                    # 对话资源文件
│   │   │   ├── 游戏开始.asset
│   │   │   ├── 第2日起床.asset
│   │   │   └── ...
│   │   └── Character/                 # 角色预制体
│   │       ├── C1.prefab
│   │       ├── C2.prefab
│   │       └── C3.prefab
│   └── StreamingAssets/
│       └── Data/
│           ├── character.csv          # 角色数据表
│           ├── daily.csv              # 每日对话数据表
│           └── map.csv                # 地图场景数据表
```

### 关键概念

- **DialogGraph**: 对话图表，包含多个对话节点，定义完整的对话流程
- **DialogNode**: 单个对话节点，包含对话文本、选项、奖励、条件等
- **Character**: 游戏中的NPC角色
- **CharacterCA**: 角色的配置数据（Configuration Asset）
- **CSV数据表**: 存储角色基础信息的数据表

---

## 添加新人物

### 步骤1：准备角色3D模型或预制体

1. 在Unity中创建角色的3D模型或2D精灵
2. 为角色添加必要的组件（如动画控制器、碰撞体等）
3. 将角色保存为Prefab预制体

**文件位置**: `Assets/Resources/Character/`

**命名规范**: 使用`C`+数字的格式，例如：`C4.prefab`, `C5.prefab`

### 步骤2：在CSV数据表中添加角色信息

打开文件：`ProjectUnity/Client/Assets/StreamingAssets/Data/character.csv`

按照以下格式添加新行：

```csv
ID,名称,地址
cid,name,path
1200001,少女,Character/C1
1200002,市长凯恩,Character/C2
1200003,牧场老板李北镇,Character/C3
1200004,新角色名称,Character/C4  ← 添加新角色
```

**字段说明**：
- **ID** (cid): 角色唯一标识符，建议使用120xxxx格式
- **名称** (name): 角色在游戏中显示的名称
- **地址** (path): 角色预制体在Resources文件夹中的路径（不含.prefab后缀）

### 步骤3：在场景中放置角色

如果需要将角色放置在特定场景中，有两种方式：

**方式A：直接在场景中放置**
1. 打开目标场景（如`Assets/Scenes/Map/Shop.unity`）
2. 将角色预制体拖入场景
3. 调整位置和朝向
4. 为角色添加点击交互功能（添加Collider和脚本）

**方式B：通过脚本动态生成**
在相应的场景管理器中使用CharacterFactory创建角色：

```csharp
CharacterFactory cf = CBus.Instance.GetFactory(FactoryName.CharacterFactory) as CharacterFactory;
Character character = cf.Create(1200004) as Character; // 使用新角色的ID
```

### 步骤4：创建角色对话内容

角色创建完成后，需要为其添加对话内容（参见"添加新对话"章节）。

---

## 添加新对话

### 方法1：使用XNode编辑器创建对话图（推荐）

#### 步骤1：创建DialogGraph资源

1. 在Unity项目窗口中，右键点击`Assets/Resources/Dialog/`文件夹
2. 选择 `Create > DialogSystem > DialogGraph`
3. 命名对话图，例如：`新角色对话` 或 `第4日起床`

**命名规范**：
- 角色对话：`{角色名}{角色ID}/{角色ID}_{场景名}_{日期}`
  - 格式为直接将角色名和ID连接（无分隔符）
  - 例如：`少女1200001/1200001_Shop_1`（少女是角色名，1200001是ID）
  - 也可使用英文：`Blacksmith1200004/1200004_Shop_1`
- 每日剧情：`第{X}日{时段}`（中文格式）
  - 例如：`第4日起床`, `第5日晚上`
  - 也可使用英文：`Day4Morning`, `Day5Night`

#### 步骤2：使用XNode编辑器编辑对话

1. 双击创建的DialogGraph资源，打开XNode编辑器窗口
2. 右键点击空白区域，选择 `Add Node > Dialog Node`
3. 配置对话节点的各项参数

#### 步骤3：配置DialogNode节点参数

每个DialogNode节点包含以下可配置字段：

##### 基础字段
- **input**: 输入端口（用于连接上一个节点）
- **speakerid**: 说话者ID（对应character.csv中的角色ID，0表示旁白）
- **ani**: 动画名称（可选，用于触发角色动画）
- **dialogText**: 对话文本列表（可以添加多条，按顺序显示）
  - 点击`+`号添加新的对话文本
  - 支持多行文本（使用TextArea显示）

##### 奖励与消耗
- **rewards**: 对话完成后获得的奖励列表
  - 格式：`Pair<int, int>`（资产ID, 数量）
  - 例如：获得10个金币
- **cost**: 对话所需消耗的资源
  - 格式：`Pair<int, int>`（资产ID, 数量）

##### 选项系统
- **choices**: 玩家可选择的对话选项列表
  - 每个选项包含：
    - **choiceText**: 选项显示的文本
    - **conditions**: 选项显示的条件列表
      - **id**: 属性ID
      - **comparisonType**: 比较类型枚举值
        - `GreaterThan` (>)
        - `LessThan` (<)
        - `EqualTo` (==)
        - `GreaterOrEqual` (>=)
        - `LessOrEqual` (<=)
      - **value**: 目标值

##### 场景控制
- **scene**: 对话结束后跳转的场景名称（可选）
- **prefab**: 在场景中加载的预制体名称（可选）
- **endingDescription**: 结局描述文本（如果是结局节点）
- **eventid**: 触发的事件ID（可选）

##### 输出端口
- **nextNode**: 动态输出端口，根据choices数量自动生成
  - 无选项时：只有一个默认输出端口
  - 有选项时：每个选项对应一个输出端口

#### 步骤4：连接对话节点

1. 从一个节点的输出端口拖动到另一个节点的输入端口
2. 创建对话流程图
3. 确保有明确的开始节点（graph.nodes[0]）

#### 示例对话图结构

```
[开始节点] 
    ↓
[角色打招呼]
    ↓
[玩家选择对话选项] ─→ [选项A] → [结果A]
    ├─→ [选项B] → [结果B]
    └─→ [选项C] → [结果C]
```

### 方法2：通过代码调用对话

在代码中，可以通过DialogManager调用对话：

```csharp
// 调用对话的方式1：通过对话名称
DialogManager dm = CBus.Instance.GetManager(ManagerName.DialogManager) as DialogManager;
dm.ShowDialog("游戏开始");

// 调用对话的方式2：点击角色触发
// 当玩家点击角色时，系统会自动查找对应的对话
// 对话命名格式：{角色名}{角色ID}/{角色ID}_{场景名}_{日期}
// 例如：少女1200001/1200001_Shop_2
```

### 步骤5：在每日数据中配置对话（可选）

如果对话是每日固定剧情，需要在`daily.csv`中添加：

打开文件：`ProjectUnity/Client/Assets/StreamingAssets/Data/daily.csv`

```csv
ID,名称,聊天内容,强制跳转
cid,name,dialog,scene
1400001,第1日清晨,第1日起床,
1400002,第2日清晨,第2日起床,
1400003,第3日清晨,第3日起床,
1400004,第4日清晨,第4日起床,  ← 添加新的每日对话
```

---

## 高级功能

### 条件选项系统

对话选项可以设置显示条件，只有满足条件时玩家才能看到该选项。

**示例**：只有好感度>=50时才显示特殊选项

1. 在DialogNode的choices中添加选项
2. 为选项添加Condition：
   - id: 好感度属性ID
   - comparisonType: GreaterOrEqual
   - value: 50

### 奖励和消耗系统

对话可以给予玩家奖励或消耗资源。

**配置方法**：
1. 在DialogNode的`rewards`字段添加奖励项
   - 这是一个`Pair<int, int>`列表，其中：
     - 第一个值(k): 资产ID（参考asset.csv）
     - 第二个值(v): 数量
2. 在DialogNode的`cost`字段添加消耗项（格式相同）

**效果**：
- 对话显示时自动处理奖励/消耗
- 在屏幕上显示提示信息
- 更新玩家背包数据

### 场景跳转

对话可以在结束时自动跳转到新场景。

**配置方法**：
1. 在DialogNode的`scene`字段输入目标场景名称
2. （可选）在`prefab`字段输入要加载的预制体名称

**示例**：
```
scene: Shop
prefab: LobbyPanel
```

### 事件触发

对话结束时可以触发游戏事件。

**配置方法**：
1. 在DialogNode的`eventid`字段输入事件ID
2. 事件会由EventManager处理

### 对话打字机效果

系统默认启用打字机效果（逐字显示对话文本）。

**调整速度**：
- 在DialogPanel.cs中修改`typingSpeed`变量
- 默认值：0.05秒/字符

### 对话历史记录

系统会自动保存已完成的对话。

**机制**：
- 使用PlayerPrefs保存
- 键名格式：对话图表名称
- 值：endingDescription的内容
- 重复触发时直接显示保存的结局描述

---

## 完整示例：添加新角色并配置对话

### 场景：添加一个名叫"铁匠"的新NPC

#### 1. 创建角色预制体
- 在Unity中创建铁匠的3D模型
- 保存为`Assets/Resources/Character/C4.prefab`

#### 2. 添加角色数据
在`character.csv`中添加：
```csv
1200004,铁匠,Character/C4
```

#### 3. 创建对话图
- 创建`Assets/Resources/Dialog/铁匠1200004/1200004_Shop_1.asset`
- 打开XNode编辑器

#### 4. 配置对话节点

**节点1：打招呼**
```
speakerid: 1200004
dialogText: ["你好，欢迎来到铁匠铺！"]
choices: [继续对话]
```

**节点2：选择对话主题**
```
speakerid: 1200004
dialogText: ["需要我帮你什么吗？"]
choices: 
  - "我想买武器" → 节点3
  - "我想升级装备" → 节点4
  - "只是路过" → 节点5
```

**节点3：购买武器**
```
speakerid: 1200004
dialogText: ["这把剑很适合你，只要100金币。"]
cost: [(金币ID, 100)]
rewards: [(剑ID, 1)]
choices: []  # 对话结束
endingDescription: "你购买了一把新剑"
```

#### 5. 连接节点并保存

#### 6. 在场景中放置角色
- 打开Shop场景
- 将C4预制体拖入场景
- 调整位置

#### 7. 测试
- 运行游戏
- 点击铁匠角色
- 验证对话是否正确显示

---

## 注意事项

1. **ID规范**：
   - 角色ID：120xxxx
   - 每日数据ID：140xxxx
   - 资产ID：参考asset.csv

2. **文件命名**：
   - 对话资源文件可使用中文或英文命名
   - 角色对话必须遵循命名格式`{角色名}{角色ID}/{角色ID}_{场景名}_{日期}`才能被系统识别
   - 系统会在Resources/Dialog文件夹中按照此路径查找对应文件

3. **对话流程**：
   - 确保对话图有明确的结束节点
   - 没有choices的节点会自动关闭对话面板

4. **资源路径**：
   - Resources文件夹中的路径不需要包含扩展名
   - 使用正斜杠`/`作为路径分隔符

5. **测试建议**：
   - 每次添加新内容后及时测试
   - 检查对话是否正确触发
   - 验证奖励和条件系统是否正常工作

6. **版本控制**：
   - 修改CSV文件前先拉取最新分支
   - 避免覆盖他人的修改
   - 及时提交和合并更改

---

## 常见问题

### Q: 对话无法触发？
A: 检查以下几点：
1. 对话资源文件是否在Resources/Dialog文件夹中
2. 文件命名是否符合格式要求
3. DialogGraph是否正确配置了节点
4. 角色ID是否在character.csv中存在

### Q: 角色模型不显示？
A: 检查：
1. 预制体路径是否正确
2. speakerid是否对应正确的角色ID
3. 角色Prefab是否在Resources/Character文件夹中

### Q: 对话选项不显示？
A: 检查：
1. choices是否正确配置
2. 条件系统是否阻止了选项显示
3. UpdateDynamicPorts是否被正确调用

### Q: 如何调试对话系统？
A: 
1. 在DialogPanel.cs中添加Debug.Log语句
2. 检查Unity Console的错误信息
3. 使用XNode编辑器检查节点连接

---

## 相关文件速查表

| 功能 | 文件路径 |
|------|----------|
| 角色数据配置 | `Assets/StreamingAssets/Data/character.csv` |
| 对话资源 | `Assets/Resources/Dialog/` |
| 角色预制体 | `Assets/Resources/Character/` |
| 对话系统脚本 | `Assets/Scripts/Dialog/` |
| 角色系统脚本 | `Assets/Scripts/Game/Character/` |
| 对话UI面板 | `Assets/Scripts/Panel/Dialog/DialogPanel.cs` |
| 每日剧情配置 | `Assets/StreamingAssets/Data/daily.csv` |
| 地图场景配置 | `Assets/StreamingAssets/Data/map.csv` |

---

## 结语

本指南涵盖了在项目中添加新对话和新角色的所有基本步骤。如有任何疑问，请参考项目现有的对话和角色实现，或在开发者群中提问。

祝开发顺利！🎮
